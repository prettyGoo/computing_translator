begin
    loop break;
    loop write 1;
    loop read i;
    loop (i[1] i :=);
    loop (i b[i] cast);
    loop write (1 i mod), i[1], i;
    loop;
    loop begin break; end;
    begin loop break; end;
    loop loop loop;
    loop begin
        loop loop;
    end;
    loop begin
        loop;
        case 1 of 1:;
        read i, b;
        write (1 1 +);
        write (i i +);
        write (i i -);
        write (i -);
    end;
    loop call abc end;
    loop goto 01;
end