begin
    loop break;
    loop write 1;
    loop read i;
    loop (i[1] i :=);
    loop (i b[i] cast);
    loop;
    loop begin break; end;
    begin loop break; end;
    loop loop loop;
    loop begin
        loop loop;
    end;
    loop begin
        loop;
        case 1 of 1:;
        (i i cast);
        (i 1e+5 +);
    end
end